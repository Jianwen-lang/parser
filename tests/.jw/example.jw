____________________
------[title]=JianWen 语法全集示例
-----[author]=UE-DND
-[author_url]=https://github.com/UE-DND
-------[time]=2025年11月20日
---[add_info]=本文件覆盖了 docs/grammar.md 中的全部语法
-----[tag(s)]=语法,示例,全集
[global_font]=mono
____________________

### 标题

# 一级标题
## 二级标题
### 三级标题
#### 四级标题
##### 五级标题
#####+ 可折叠标题

这是第一个可折叠区块。

[fold]
这是第二个也会被折叠的区块。

=====

内容标题示例所在区块
> 这是一个内容标题

### 文本样式

行内样式：*加粗*、/斜体/、_下划线_、-删除线-、~波浪线~、^上标^、^^下标^^。
组合样式：/*_同时加粗斜体下划线_*/。
禁用行内解析：{*这里不会加粗* /也不会倾斜/}，以及使用 \* 保留星号，如果要显示大括号，可以使用 {{}。

[tag=高亮效果]
### 高亮效果

行内框选高亮：这里有``框选高亮``。
行内荧光笔高亮：这里有=荧光笔高亮=。

``
这是一个应用框选效果的区块。
可以有多行内容。
``
> 框选区块的内容标题

### 引用块

@ 这是一个引用块的第一层
@@ 这是一个嵌套的引用块

行内代码示例：请使用 `printf("hi")` 输出。

```C
#include <stdio.h>
int main() { return 0; }
```
> C 代码示例

## HTML 代码

[html]
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
	<meta charset="UTF-8">
	<title>音乐播放器</title>
	<style>
		.music-player {
			width: 350px;
			padding: 20px;
			border: 1px solid #ccc;
			border-radius: 8px;
			background-color: #f7f7f7;
			box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.1);
			font-family: Arial, sans-serif;
			text-align: center;
			margin: 50px auto;
		}
		h2 {
			color: #333;
			margin-top: 0;
			font-size: 1.2em;
		}
		p {
			color: #666;
			font-size: 0.9em;
		}
		audio {
			width: 100%;
			margin-top: 15px;
		}
	</style>
</head>
<body>
	<div class="music-player">
		<h2>音乐播放器</h2>
		<p>当前播放：示例音轨</p>
		<audio controls>
			<source src="path/to/your/music.mp3" type="audio/mpeg">
			您的浏览器不支持音频元素。
		</audio>
	</div>
</body>
</html>
```

普通无序列表：
- 第一项
-- 第二项（缩进一层）
--- 第三项（缩进两层）
- 第四项

清单列表与状态：
-[] 未知状态
-[o] 进行中
-[x] 未完成
-[v] 已完成

有序列表与状态：
1. 第一步
1.1 第二步的子步骤
1.1.1[x] 带有未完成标记的子步骤
2.[o] 带有进行中标记的第二步

可折叠列表：
+ 任务1
++[v] 已完成的子任务
+++[x] 未完成的更深层子任务
+[o] 正在进行的任务2

# 分割线示例
上方是标题，下方是不同风格的分割线。
-----
*****
=====
~~~~~

属性标识符改变文本样式：
[red]这一行文字为红色。
[green,bold]这一行文字为绿色加粗。
[blue][italic]这一行文字为蓝色斜体。
[b]粗体别名，[i]斜体别名，[bb]特粗体别名。
[heavy]特粗体 [serif]衬线体 [mono]等宽体 [slim]细体。
[2]这一行使用字号 2。[/] 普通字号恢复。

	[disable]
	这一整个区块的行内语法 *不会* 被解析，`代码` 也只是普通文本，尝试下 {}。

[d]
这一整个区块同样禁用语法解析。

区块缩进与截断示例：
区块A 位于默认起始位置 L，下方区块应用 [->]。

	[->]
	区块B 起始位置为 C，并与上方区块同一行展示。

		[->]
		区块C 起始位置为 R，并与上方区块同一行展示。

[<-]
应用 [<-] 后，过长长长长长长长长长长长长长长长长长长长长长长长长长长长的内容会在边界处截断。

	[<->]
	同时使用 [<->] 既会缩进，过长长长长长长长长长长长长长长长长长长长长的内容又会在边界处截断。

[c]
这个区块整体居中对齐。

[r,<-]
这个区块整体右对齐。

[tag=对齐示例]
这是一个被打上标签的区块，可以通过 [@=对齐示例] 再次引用。

[t=短标签示例]
这是使用 [t=name] 形式定义的标签区块。

[@=对齐示例]
[@=短标签示例]

[sheet]
| 模块 | 状态 | 说明 |
| 语法文档 | 已完成 | 基本语法 |
| 解析器 | 已完成 | AST 与语义 |
| HTML 渲染器 | 进行中 | 样式细节 |

	[sheet,r]
	| 姓名 | 年龄 | 性别 |
	| A | 15 | 女 |
	| B | 23 | 男 |
	| C | 44 | 男 |

		[sheet][r]
		| 城市 | 人口 | 地区 |
		| X市 | 100 | 华北 |
		| Y市 | 200 | 华南 |

[sheet]
| 名称 |[r] 数值 |[r] 单位 |
| A | 10 | 次 |
| B | 20 | 次 |

表格兼容性语法对齐示例：

[sheet]
|:---|:---:|---:|
| 左对齐 | 居中 | 右对齐 |
| 1 | 2 | 3 |

高亮进阶与边框样式：
需要[green]``绿色边框框选``的示例。

[#FFEB3B,!blue,b]``
这是一个 block 级框选示例，带有黄色边框和蓝色填充，并加粗。
``

需要[red]=*使用荧光笔高亮*=的示例。
[square]``方形边框的框选`` 示例。

线条进阶示例：
内容[red]-这段文字的删除线为红色-，以及蓝色分割线

[blue]=====

#+ 可折叠标题进阶

[fold]
这个区块会被折叠。

[fold]
这个区块也会被折叠。

普通区块不会被上述可折叠标题折叠。

超链接文本示例：
点击[link]此处(https://www.google.com)打开 Google。
点击[link]此处(files/logo.png)查看本地图片。
点击[link]此处(### 分割线示例)跳转到分割线标题。

在可解析符号前应用 [color] 以改变线条颜色，如下划线、删除线、波浪线、分割线。

点击[link]此处(高亮效果)跳转到高亮效果说明区块。

点击[green,!red]此处(https://www.google.com)打开带有自定义颜色的链接。
点击[green][!red]此处(https://www.google.com)再次测试链接颜色配置。

脚注与参考文献示例[fn:1]，同一脚注也可以被多次引用[fn:1]，也可以使用[fn:RefA] 这样的字符串 ID。

[footnotes]
[fn=1]
这里是脚注 1 的详细说明，可以包含行内样式和链接等内容。
[fn=RefA]
作者. 文章标题. 出版信息, 年份。

批注与评论示例：
后面隐藏了一句话。[comment]这里是仅作者可见的说明[/]前面隐藏了一句话，仅作者可见。

[comment]
这一整个区块都是批注内容，在最终渲染中通常会被隐藏。

图片示例：
[img](image.png)
> 一张示例图片

	[rounded=2,img](image.png)
	> 自定义圆角曲率的示例图片

HTML 文件示例：
[html](files/sample.html)

内容引用示例：
以下内容来自另一篇 JianWen 文档：
[@](guide.jw)
